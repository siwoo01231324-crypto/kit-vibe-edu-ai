# hooks/ — .ai.md

## 목적
공유 React 커스텀 훅 모음. 주로 클라이언트 컴포넌트에서 사용.

## 파일
- `useSessionStatus.ts` — Supabase Realtime으로 sessions 테이블의 status 변화를 구독. 대기 화면에서 사용.

## 규칙
- 모든 훅은 `'use client'` 지시어 필요
- Supabase 구독은 unmount 시 반드시 `removeChannel` 정리
